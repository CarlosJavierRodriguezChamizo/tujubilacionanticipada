---
import type { CollectionEntry } from 'astro:content';
import ArticleCard from './ArticleCard.astro';
import AvisoIA from './AvisoIA.astro';

interface Props {
  posts: CollectionEntry<'blog'>[];
  currentPage: number;
  lastPage: number;
}

const { posts, currentPage, lastPage } = Astro.props;

// Construye la URL de una página del listado (página 1 = /blog).
const pageHref = (n: number) => (n <= 1 ? '/blog' : `/blog/page/${n}`);
const hasPrev = currentPage > 1;
const hasNext = currentPage < lastPage;
---

<!--
  Divulgación de IA también en el listado: el art. 50.5 del Reglamento (UE)
  2024/1689 exige informar «a más tardar» en la primera exposición, y para
  quien solo hojea los titulares la primera exposición es esta página.
-->
<AvisoIA variante="insignia" class="mb-6" iaTexto={true} revisado={true} />

<div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
  {posts.map((post) => <ArticleCard post={post} />)}
</div>

{
  lastPage > 1 && (
    <nav
      class="mt-12 flex items-center justify-between border-t border-paper-300 pt-6"
      aria-label="Paginación"
    >
      {hasPrev ? (
        <a
          href={pageHref(currentPage - 1)}
          class="btn-secondary no-underline"
          rel="prev"
        >
          ← Anteriores
        </a>
      ) : (
        <span class="btn-secondary cursor-not-allowed opacity-40" aria-disabled="true">
          ← Anteriores
        </span>
      )}

      <span class="text-sm text-ink-muted">
        Página {currentPage} de {lastPage}
      </span>

      {hasNext ? (
        <a
          href={pageHref(currentPage + 1)}
          class="btn-secondary no-underline"
          rel="next"
        >
          Siguientes →
        </a>
      ) : (
        <span class="btn-secondary cursor-not-allowed opacity-40" aria-disabled="true">
          Siguientes →
        </span>
      )}
    </nav>
  )
}
